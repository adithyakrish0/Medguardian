# Render.com Blueprint for MedGuardian
# =====================================
# Deploy with: Connect GitHub repo → Select "render.yaml" as blueprint

services:
  - type: web
    name: medguardian-api
    runtime: docker
    dockerfilePath: ./Dockerfile.render
    plan: free
    region: oregon
    healthCheckPath: /health
    envVars:
      - key: FLASK_ENV
        value: production
      - key: VISION_DISABLED
        value: "true"
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: medguardian-db
          property: connectionString
      # Add these manually in Render dashboard:
      # - SUPABASE_URL
      # - SUPABASE_KEY
      # - GEMINI_API_KEY
      # - TELEGRAM_BOT_TOKEN

databases:
  - name: medguardian-db
    plan: free
    databaseName: medguardian
    user: medguardian
